@model TeamRoleForm

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title">
        Team Role
    </h4>
</div>
<div class="modal-body">
    <form id="teamrole-form">
        <input type="hidden" name="TeamRoleId" value="@Model.TeamRoleId" />
        <input type="hidden" name="PhysicianId" value="@Model.PhysicianId" />
        <div class="form-group">
            <label class="control-label">Name</label>
            <div>
                @Html.TextBox("Name", Model.Name, new { @class = "form-control" })
                @Html.ValidationMessage("Name", "", new { @class = "text-danger" })
            </div>
        </div>
    </form>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
    <button type="submit" onclick="@(Model.TeamRoleId.HasValue ? "saveEditTeamRoleForm" : "saveNewTeamRoleForm")($('#teamrole-form'))" class="btn btn-primary">Save</button>
</div>