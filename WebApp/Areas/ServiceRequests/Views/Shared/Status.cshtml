@model LookupViewModel<Enums.ServiceRequestStatus>

@if (Model.Id != Enums.ServiceRequestStatus.Active)
{
    <span class="label" style="color:white;background-color:@Model.ColorCode"><strong>@Model.Name</strong></span>
}