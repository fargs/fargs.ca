@using DanTup.Web
@model IEnumerable<WebApp.Views.Comment.CommentViewModel>

@foreach (var comment in Model.OrderByDescending(c => c.PostedDate))
{
    @Html.Partial("~/Views/Comment/_Comment.cshtml", comment)
}
