@model bool

@if (Model)
            {
    <div class="form-group">
        <div class="alert alert-warning">
            <span>It is recommended that this cancellation be marked as late</span>
        </div>
    </div>
}